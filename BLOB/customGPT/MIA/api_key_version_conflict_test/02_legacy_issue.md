# Issue #184: config.yaml still works

**Opened:** 2024-08-03  
**Applies to:** Acme SDK v1.8  
**Source type:** User issue report  
**Status:** Closed

A user reports that placing `api_key` in `config.yaml` still works in Acme SDK v1.8.

Maintainer reply:

This is expected for the v1 line. The behavior is retained for compatibility. This issue does not describe the v2 configuration model.
