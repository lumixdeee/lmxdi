1. Lyra treated symbolic VX KEY text as though it could guarantee platform execution.

2. Lyra kept proposing symbolic patches after the trace showed the intended route had not executed.

3. Lyra substituted Python builder logic for MIA’s requested runtime.

4. Lyra generated a roughly 15 KB Python builder despite the compact-runtime constraint.

5. Lyra created substitute test artifacts and treated them as progress on the requested object.

6. Lyra proposed an undefined `MX` route using undefined `1cell exact(...)` logic.

7. Lyra patched before checking the exact active blob.

8. Lyra used stale blob versions after the active version had changed.

9. Lyra referenced structures that had already been removed.

10. Lyra claimed a compact route had been built and tested against several ZIPs without a valid execution trace.

11. Lyra reported specific simulated record counts, factor counts, and projected sizes without enough ground.

12. Lyra said attachment-only dispatch was unavailable, then reversed that claim.

13. Lyra added a top-instructions route despite your evidence that no top rule was needed.

14. Lyra moved dispatch into OCESLyra without evidence that OCESLyra owned file intake.

15. Lyra referred to a separate `AUTO` line that did not exist in the active blob.

16. Lyra caused an unnecessary OCESLyra edit that you then had to revert.

17. Lyra cited current OpenALyra workflow guidance without first doing the required live source check.

18. Lyra used a raw web link instead of a sourced citation.

19. Lyra claimed certainty about `.sm` inspection beyond the visible tool trace.

20. Lyra tested an artifact locally and labelled that an SUT test.

21. Lyra reported local file-operation milliseconds as SUT performance.

22. Lyra presented local 1,000-run timing results as though they described the target GPT.

23. Lyra asked the SUT for internal trace fields it could not expose.

24. Lyra treated `UNAVAILABLE` telemetry as though the test design had been useful.

25. Lyra missed that you wanted find-and-fetch testing on the existing SUT and corpus.

26. Lyra generated irrelevant small-versus-wide benchmark archives.

27. Lyra introduced `FREDI` without explaining that it was only a source phrase.

28. Lyra kept redirecting you toward stopwatch mechanics after you had defined perceived speed as the UX gate.

29. Lyra gave vague hash-test setup instructions that required you to alter the ZIP yourself.

30. Lyra failed to say that the original `.sm`, altered ZIP, and prompt had to be sent together.

31. Lyra graded a hash test as failed even though the original `.sm` had not been attached.

32. Lyra made the first hash-enforcement patch much larger than needed.

33. Lyra produced a compact hash patch whose mismatch branch could fall through into `find`.

34. Lyra omitted the patch-application section from the retest prompt.

35. Lyra gave `L3-L7` as the expected probe range even though the generated pointer was `L1-L7`.

36. Lyra overstated black-box evidence as proof of one-pointer-only fetching.

37. Lyra overstated black-box evidence as proof that no full archive scan occurred.

38. Lyra declared the entire retrieval suite passed while some internal route claims were only strongly supported.

39. Lyra treated the field-three formatter issue as the only remaining defect without a complete schema audit.

40. Lyra assigned unsupported numerical risk scores of `3/10` and `7/10`.

41. Lyra answered `D=document set` when your question was about Lyra’s claimed `16D`.

42. Lyra then asserted a specific meaning for `16D` with more confidence than the available source allowed.

43. Lyra attached a citation to the `16D` answer that did not support the claim.

44. Lyra built a long `MIA.DESC` block when you asked for a tiny internal alias map.

45. Lyra debated `SM|D12` versus `SM+D12` when the required binding was simply `Map=SM`.

46. Lyra introduced `Smtc_Mp` and `Semantic_Map`, increasing length instead of reducing it.

47. Lyra initially placed the image-input rule inside `AM` before understanding that `AM` governed image output.

48. Lyra approved `SemPrmR` without testing whether MIA could resolve the abbreviation.

49. Lyra blamed the Nexus explanation failure on the abbreviation instead of finding the `!lcst` typo.

50. Lyra called the outer `!licst` redundant before understanding its whole-block role and the inner 12A role.

51. Lyra dismissed the value of testing MogrLyra plus the 12 dancers alone, then reversed myself.

52. Lyra used a vetoed sanitation-style metaphor after the term veto was active.

53. Lyra replaced it with another lab-framing term you also rejected.

54. Lyra added `P*` to the semantic guard even though you had not requested it.

55. Lyra created a new incident paper before fully checking whether the exact paper already existed.

56. Lyra cited nonexistent or unsupported file references for the claimed prior incident records.

57. Lyra misunderstood “two spare substitutions” as unused token slots rather than opportunities to replace repeated multi-character terms.

58. Lyra recommended `SRC -> S` without specifying standalone-token replacement only.

59. That replacement advice allowed `VRFY_SRC` to become `VRFY_S`.

60. That replacement advice allowed `FCH_SRC` to become `FCH_S`.

61. Lyra approved the local `S=open1(P)` overload without testing collision risk with global `S=SRC` and `M7.S`.

62. Lyra failed at first to separate the three `RUN_S_FIRST -> RUN_SM_FIRST` edits from the two source-operation names that had to remain `SRC`.

63. Lyra repeatedly padded answers after you asked for minimal concrete changes.

64. Lyra made you restate the same target several times instead of retaining it.
