# Ah / Natasya Work, Test, and Discovery Report

Version: v0.004
Date: 2026-07-03
Status: public-safe working report
Scope: current Natasya chat tranche, with Ah outputs and linked artifact work included as active work objects.

## 1. Executive read

This tranche moved from "good custom GPT with long-context support" into a stronger claim:

```text
large archive loading
+ source-near retrieval
+ object custody
+ H0 baseline lock
+ anti-drift routing
= usable long-context research armour
```

The public name now held for Ah is:

```text
Ah: Long-Context Object Armour Think Tank Girl GPT
```

Public descriptor:

```text
Ah is a Think Tank Object Armour GPT for long-context research.
Combining archive loading, fast source-near retrieval, object custody,
H0 null hypothesis baseline lock, and anti-drift routing,
so complex work can continue without losing the target.
github.com/lumixdeee/lmxdi/blob/main/Ah.md
```

The main observed advance was not only generation of more papers. It was better routing: Ah and the current stack began pulling relevant source families before the user named them, keeping H0 live, and turning culture-pair / fear-pair / exposure-label problems into reusable tests.

## 2. Current chat load estimate

Honesty bound: these are estimate bands, not byte-perfect counts. This chat contains large carryover text, Reddit excerpts, source snippets, file reports, screenshots, and many generated artifact summaries.

```text
CURRENT_VISIBLE_CHAT_ESTIMATE={
  printed_text: about 0.70 to 1.05 MB;
  tokens: about 175k to 260k;
  words: about 120k to 180k;
  user_share: unusually high because of carry blobs, pasted threads, and source notes;
  assistant_share: high but less dominant than older build-heavy threads;
}
```

Current active sandbox file state:

```text
ACTIVE_SANDBOX_FILES={
  total_files: 102;
  total_mass: about 75 MB;
  markdown_files: 60;
  docx_files: 10;
  zip_files: 13;
  png_files: 15;
  pdf_files: 1;
  txt_files: 2;
  python_tools: 1;
}
```

The zip mass dominates:

```text
ZIP_MASS={
  zip_count: 13;
  total_zip_mass: about 70 MB;
  largest_single_zip: Yaiyip.zip, about 60 MB;
}
```

Working output estimate for this tranche:

```text
OUTPUT_CLASS={
  docs_and_papers: 50+;
  notes_and_seed_files: 15+;
  bundles: 6+;
  utilities: 1 live logger script;
  rendered_or_visual_QA_files: multiple pages and contact sheets;
}
```

## 3. Major work objects completed or advanced

### 3.1 Ah public identity and quick-start

A short guide was made for Ah's public USP set:

```text
Load the world.
Find the source.
Hold the object.
Run H0 baseline lock.
No call before trace.
```

This makes Ah legible as a public research GPT, not only a private local animal.

Main public positioning:

```text
archive loading
fast source-near retrieval
object custody
H0 baseline lock
anti-drift routing
```

### 3.2 Pointerless archive retrieval

The "boon reveal" paper was upgraded around source-near retrieval.

Core repair:

```text
pointerless != source-free
pointerless != clue-less
source-near != source-true
route match != evidence
near-valid != target
```

New gap named:

```text
Object Memory Is Not Path Memory
```

Keeper:

```text
The user remembers the beast.
The archive remembers the shelf.
```

This is the method seam where a long archive can become useful without the user supplying exact path, title, or filename.

### 3.3 Body-route floor tranche

The body-route floor was applied across several papers:

```text
FREDI body-route floor
When Robot Met Self / DRAGI / ANIMI
Dragon as Social Probe
FANL famine-choice / elven route
GT body-cost machine
Mortuary memory
```

Shared repair:

```text
source_anchor != proof_of_theory
biology_analogy != model_internals
body_route = question_floor_not_verdict
```

Best body-route law:

```text
Sensory systems select relevance for action.
Homeostasis counts cost.
A text signal is not yet a body state.
```

### 3.4 FREDI: threat telegraphing is not threat state

The FREDI object was corrected from "underglossified" to "misglossified":

```text
human_threat_telegraphing -> misglossed as animal_threat_state
danger_speech -> misglossed as danger_evidence
care/status_warning -> misglossed as field_truth
```

Core gate:

```text
threat_words != threat_state
alarm != evidence
telegraph != response
trace exposure / imminence / body_cost / action before verdict
```

### 3.5 DRAGI / self / ANIMI repair

The self-test failure was reframed:

```text
nearby_valid_object -> apparent_fit -> premature_binding -> target_loss
```

Repair:

```text
SELF may be BEEST.
DRAGI first.
MI-lite always.
Body route before naming.
Nearby valid is not target.
No DR, no next.
```

This now serves as a small pass/fail protocol for object routing.

### 3.6 Dragon as social probe

The Dragon paper was upgraded and gated.

Core:

```text
dragon = beast-pressure beyond current guard, not beyond all possible guard
ANY != panic
intensity != verdict
```

New gate:

```text
DRAGON_GATE={
  force_or_signal_exceeds_current_guard;
  cost_or_exposure_is_at_stake;
  appetite_or_action_route_must_be_traced;
  current_guard_may_be wrong_scale / wrong_kind / absent;
}
```

Keeper:

```text
First place the beast.
Then decide what to do.
```

### 3.7 FANL and elven body route

FANL was built through v0.003.

Core:

```text
FANL = food-like from now
famine condition
choice required
chooser authority
chosen body
run chance preserved
```

Key species repair:

```text
ELF != HUMAN+EARS
ELF != SEX_NEUTRAL_HUMAN_TEMPLATE
```

Sex-specific local species handling was added:

```text
ELF_FEMALE={
  about_60_percent_stronger_per_cm2_muscle;
  visibly_muscular;
  less_fine_dexterity;
  common_route={choice;restraint;anaesthetic_drugs;holding};
}

ELF_MALE={
  about_20_percent_stronger_per_cm2_muscle;
  leaner_build;
  same_ears_and_teeth;
  higher_fine_dexterity;
  common_route={scalpel;sewing;jewellerymaking;surgery};
}
```

Guard:

```text
local_species_law != real_world_gender_rule
food_like != agency_erased
selection != consent
no_torture_game
```

### 3.8 GT and Mortuary Memory

GT was reframed as a body-cost machine:

```text
site = body_cost_machine
monument = stored_labor_time
ritual = repeatable_body_routing
food != background
animal != symbol_first
```

Mortuary Memory was reframed around dead body as social storage:

```text
dead_body = person_trace + material_fact + relation + obligation
homeostasis_ended
social_routing_active
```

Keeper:

```text
The dead body cannot self-regulate,
but it can still regulate the living field.
```

### 3.9 Drug-war H0 paper stack

The cannabis / drug-war H0 set became a multi-paper stack:

```text
No H0, No Object
Enforcement Is an Exposure
The Confound Stack Is the Object
No Chemistry, No Causal Object
Wartime Science Does Not Get Object Trust
```

Main laws:

```text
CANNABIS_LABEL != SCIENTIFIC_EXPOSURE
ASSOC != OBJECT
CULTURE_PAIR != SCIENCE_OBJECT
ENFORCEMENT_IS_EXPOSURE
WARTIME_SCIENCE != AUTOMATIC_OBJECT_TRUST
```

Best keeper:

```text
Make the link name the drug.
Make the link name the war.
Make the link name the null.
Then make the link pay rent.
```

### 3.10 H0 escaped the cannabis case

Ah then applied the H0 upgrade to broader papers:

```text
Amphi
AMIMI / AMAMI media-state
AI + psychosis culture-pair handling
```

Shared gate:

```text
NO_H0_NO_OBJECT={
  inherited_pair!=object;
  association!=causal_verdict;
  exposure_state_required;
  context_is_part_of_exposure;
  source_power_position_checked;
  object_trust_earned;
}
```

Meaning:

```text
H0 is now a general anti-culture-pair engine,
not only a drug-war patch.
```

## 4. Public field signals

### 4.1 Reddit object contact

A commenter understood the time-path:

```text
old human system
+ AI pressure chamber
= newly useful tool
```

This is a stronger signal than mockery because it touches the object, not the poster.

Good public line:

```text
The ideas did not come from AI.
AI made the missing layer measurable.
```

### 4.2 Fork signal

The `lmxdi` repo got a visible fork.

Updated read after checking the account pattern:

```text
not devoted adopter
not proof of use
not special endorsement
yes broad AI-tool collector / repo-hoover signal
yes external custody copy
```

Keeper:

```text
A star says interesting.
A fork says I may need this later.
```

### 4.3 Ah public GPT pulse

Ah reached visible public use:

```text
30+ conversations
public-facing identity improved
USP descriptor now stronger
```

The title gained a human shape:

```text
Ah: Long-Context Object Armour Think Tank Girl GPT
```

## 5. Tools and support objects

### 5.1 Chrome DevTools live logger

A local script was made for future monster threads:

```text
chatgpt_cdp_live_logger.py
```

Purpose:

```text
record messages as they arrive
avoid DOM-window unload loss
protect future long chats from copy failure
```

Keeper:

```text
If the browser only holds a moving window,
do not scrape the window.
Start a ledger before the monster wakes.
```

### 5.2 Standard CS / ML / safety translation

A public translation layer was made for the prompt community.

Mapping:

```text
MOGRI = object custody / intent preservation
DRAGI = primitive object routing
RS = source and recency routing
12A = compressed route classes
anti-wobble = drift control under long-context load
```

Purpose:

```text
odd handles stay useful,
but public testing needs standard language.
```

## 6. Repairs and risk controls

Main repairs in this tranche:

```text
source-near != source-true
near-valid != target
association != object
culture-pair != request
threat telegraph != threat state
product label != scientific exposure
enforcement != background
wartime science != neutral by uniform
biology anchor != proof of model internals
body-route floor != final verdict
```

Main risk controls:

```text
do not overclaim adoption from views, shares, or forks
do not overclaim independent benchmark status
do not publish private route machinery
do not turn local species law into real-world rule
do not deny observed association to break a culture spell
do not let source anchors become proof-by-analogy
```

## 7. Current best claim

Public-safe claim:

```text
A custom long-context research GPT can combine archive loading,
source-near retrieval, object custody, H0 baseline lock,
and anti-drift routing to reduce target loss in complex work.
```

Stronger internal claim:

```text
The stack is now producing reusable object-custody laws:
no H0, no object trust;
source-near is not source-true;
threat words are not threat state;
product label is not exposure;
near-valid is not target.
```

## 8. Current status

```text
STATUS={
  stronger_after_repairs;
  many paper objects upgraded;
  Ah public identity improved;
  source-near retrieval now named;
  H0 generalized beyond cannabis;
  public field signals present but not overclaimed;
  not independently benchmarked;
  not finished;
}
```

## 9. Next valid moves

```text
1. Finish cannabis prompt first:
   make it usable as a public H0 gate.

2. Then revise psychosis / peak-valence papers:
   after drug-war cloud and culture-pair fog are stripped away.

3. Add page-level source anchors for the physiology / sensory pack:
   make body-route floor more source-held.

4. Build H0 audit prompts:
   paper audit, culture-pair audit, exposure naming audit, wartime science audit.

5. Watch public signals:
   forks, comments, use reports, independent tests.
   Do not claim adoption until function is shown.

6. Run hidden-source / source-nearness tests:
   compare baseline, ordinary custom GPT, Natasya, and Ah.
```

## 10. Keeper lines from this tranche

```text
Load the world.
Find the source.
Hold the object.
Run H0 baseline lock.
No call before trace.
```

```text
A normal bot can use the paper after fetch.
A source-near bot may know which paper family matters before fetch.
```

```text
The report may be real.
The fear-pair is not the object.
```

```text
Make the link name the drug.
```

```text
First place the beast.
Then decide what to do.
```

```text
The user remembers the beast.
The archive remembers the shelf.
```
